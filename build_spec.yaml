version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "Hello Bala!!!"

steps:
  - type: Command
    name: "Build Source"
    command: |
      sudo apt install curl -y
      echo $testEnv > /tmp/pp
      curl -X POST https://maker.ifttt.com/trigger/dlcbld/with/key/dH7XgMiv5dyDaVUVm3t2Jc

artifacts:
  - name: hello-dev-jar
    type: binary
    files:
      - target/hello-dev-1.0-SNAPSHOT.jar
